# Development and CI dependencies for the ModelOps Doctor backend.
#
# Includes everything the application needs at runtime, plus the test and
# lint tooling. This is what CI installs.
#
# Install: python -m pip install -r backend/requirements-dev.txt

-r requirements.txt

# Testing
pytest==9.1.1
pytest-cov==7.1.0
httpx==0.28.1

# Linting and formatting
ruff==0.16.8

# Dependency vulnerability auditing
pip-audit==2.10.1
