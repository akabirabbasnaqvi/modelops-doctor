# Runtime dependencies for the ModelOps Doctor backend.
#
# Direct dependencies only. Transitive packages are resolved by pip and
# pinned for reproducible installs in requirements.lock.txt.
#
# Install:  python -m pip install -r backend/requirements.txt
# Dev/test: python -m pip install -r backend/requirements-dev.txt

# Web framework and server
fastapi==0.139.0
uvicorn[standard]==0.51.0
python-multipart==0.0.32

# Configuration and validation
pydantic==2.13.5
pydantic-settings==2.14.2
python-dotenv==1.2.3

# Database
SQLAlchemy==2.0.54
asyncpg==0.31.0
alembic==1.18.5

# Background jobs
celery==5.6.3
redis==7.1.0

# Data and modelling
numpy==2.4.6
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.8.0
